[package]
name = "blair"
version = "1.0.1"
entrypoint = "format.typ"
authors = ["Rex Fang <i@rexylic.com>"]
license = "MIT"
description = "Rex's Typst CV Template"

[template]
path = "template"
entrypoint = "letter.typ"
